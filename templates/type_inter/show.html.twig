{% extends 'base.html.twig' %}

{% block title %}TypeInter{% endblock %}

{% block body %}
    <h1>TypeInter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_inter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_inter.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_inter_index') }}">back to list</a>

    <a href="{{ path('type_inter_edit', {'id': type_inter.id}) }}">edit</a>

    {{ include('type_inter/_delete_form.html.twig') }}
{% endblock %}
